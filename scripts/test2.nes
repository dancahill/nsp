if (string.cmp(type(_SERVER), 'table')!=0) {
	global I=0;
	for (I=0;I<1000000;I++) {
		include("tests/load.nes");
		if ((I%100)==0) {
			include("test1.nes");
			main();
			print("", I, "\r\n");
			print("this loops tens of thousands of times... just fyi...\n");
			system("ps aux | grep nesla | grep -v grep");
		}
	}
}
