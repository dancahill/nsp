#!/usr/bin/nesla

// comment

/*
multiline comment
*/

function vars() {
	if (type(_SERVER)=='table') {
		print("</FONT></PRE>");
		print(
			"<SCRIPT LANGUAGE=JavaScript>\n<!--\n",
			"function showdebug() {\n",
			"	if (document.getElementById('debug').style.display=='none') {\n",
			"		document.getElementById('debug').style.display='block';\n",
			"	} else {\n",
			"		document.getElementById('debug').style.display='none';\n",
			"	}\n",
			"}\n",
			"// -->\n</SCRIPT>\n"
		);
		print("<A HREF=javascript:showdebug()>SHOW DEBUG</A>\n");
		print("<DIV ID=debug STYLE='display:none'><PRE><FONT SIZE=2>\n");
	}
	print("global _GLOBALS = ");
	printvar(_GLOBALS);
	if (type(_SERVER)=='table') print("</FONT></PRE></DIV>\n");
	return;
}

function main() {
	function subf() { return 42; }
	include("../samples/_cards.nes");
	include("_expect.nes");
	if (type(_SERVER)=='table') {
		print("<HTML><HEAD><TITLE>NESLA WEB TEST</TITLE></HEAD><BODY>");
		print("<H1>NESLA WEB TEST</H1><PRE><FONT SIZE=2>");
	}
	print("STARTING TESTS\n");
	print("-------------------------------------------------------------------\n");
	include("_types.nes");
	print("-------------------------------------------------------------------\n");
	include("_tables.nes");
	print("-------------------------------------------------------------------\n");
	include("_scope.nes");
	print("-------------------------------------------------------------------\n");
	include("_string.nes");
	print("-------------------------------------------------------------------\n");
	include("_if.nes");
	print("-------------------------------------------------------------------\n");
	include("_loops.nes");
	print("-------------------------------------------------------------------\n");
	include("_cmp.nes");
	print("-------------------------------------------------------------------\n");
	include("_math.nes");
	print("-------------------------------------------------------------------\n");
	include("_b64.nes");
	print("-------------------------------------------------------------------\n");
	include("_rot13.nes");
	print("-------------------------------------------------------------------\n");
	include("_xml.nes");
	print("-------------------------------------------------------------------\n");
	include("_tcp.nes");
	print("-------------------------------------------------------------------\n");
	include("_ldap.nes");
	print("-------------------------------------------------------------------\n");
	include("_indexes.nes");
	print("-------------------------------------------------------------------\n");
	expect("size(CARDS)", 52, size(CARDS));
	global CARDS={};
	expect("size(CARDS)", 0, size(CARDS));
	if (type(sqlquery)=='function') Q=sqlquery("SELECT * FROM nullgs_sessions");
	if (type(dirlist)=='function') D=dirlist(_filepath);
	print("\t[", date(), " ", time(), "]\n");
	print("\truntime = ", runtime(), " seconds\n");
	print("-------------------------------------------------------------------\n");
	print("FINISHED TESTS\n");
	print("-------------------------------------------------------------------\n");
	expect("total errors", 0, tonumber(errors));

	if (type(odbc.query)=='function') {
		io.flush();
		DSN="DRIVER=MySQL;DATABASE=nullgroupware;USER=root;PASSWORD=shit";
		Q=odbc.query(DSN, "SELECT * FROM gw_users ORDER BY userid");
#		Q=odbc.query(DSN, "INSERT INTO gw_users () VALUES ();");
#		Q=odbc.query(DSN, "DELETE FROM gw_users WHERE userid > 2;");
	}
	global _ARGS={};
	global _ENV={};
	vars();
	if (type(_SERVER)=='table') print("</BODY></HTML>");
	return;
}
main();
#exit 0;

# below is code to read your /etc/passwd file into a table
#x=file.read('/etc/passwd');
#global y=string.split(x, '\n');
#for (i=0;type(y[i])!='null';i++) {
#	y[i]=string.split(y[i], ':');
#}
#printvar();
