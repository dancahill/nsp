#!/usr/bin/nesla
if (typeof(expect)!='function') include("_expect.nes");
function testindexing() {
	print("\tthis is a sample of table indexes.\n\n");
	for (i=0;i<sizeof(_GLOBALS);i++) {
		local n=iname(_GLOBALS, i);
		local t=typeof(_GLOBALS[n]);
		if (t=='function') continue;
		if (t=='table') continue;
		print("\t", t, "\t", n);
		print("\r\t\t\t\t", _GLOBALS[n], "\n");
	}
	# this will list all available string functions stored in table 'string'
#	for (i=0;;i++) {
#		local n=iname(string, i);
#		local t=typeof(string[n]);
#		if (t=='null') break;
#		print("\t", i, "\t", t, "\tstring.", n, "\n");
#	}
	return;
}
testindexing();
