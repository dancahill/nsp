#!/usr/bin/nesla
if (type(expect)!='function') include("_expect.nes");
function testindexing() {
	local i=0;

	print("\tthis is a sample of indexes.\n\n");
	for (i=0;;i++) {
		local x=_GLOBALS[@i];
		local t=type(_GLOBALS[x]);

		if (t=='null') break;
		if (t!='function') {
			print("\t", t, "\t", x);
			print("\r\t\t\t\t", _GLOBALS[x], "\n");
		}
	}
	# this will list all available string functions stored in table 'string'
#	for (i=0;;i++) {
#		local x=string[@i];
#		local t=type(string[x]);
#		if (t=='null') break;
#		print("\t", i, "\t", t, "\tstring.", x, "\n");
#	}
	return;
}
testindexing();
